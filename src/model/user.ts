export interface User {
  id: number;
  name: string;
  password: string;
  email: string;
  // firstName: string;
  // lastName: string;
  // bio: string;
  // profilePicture: string;
  // coverPhoto: string;
  // birthday: Date;
}
